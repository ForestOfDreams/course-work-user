class FavoriteItem {
  constructor(title, imageUrl, description, rating, dedlines) {
    this.imageUrl = imageUrl;
    this.title = title;
    this.description = description;
    this.rating = rating;
    this.dedlines = dedlines;
  }
}

export default FavoriteItem;
